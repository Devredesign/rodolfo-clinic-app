import React, { useEffect, useState } from 'react'
import { Alert, AppBar, Box, Button, CircularProgress, Container, Stack, Tab, Tabs, TextField, Toolbar, Typography } from '@mui/material'
import { supabase } from './supabase.js'
import ClientsModule from './ClientsModule.jsx'
import ProductsScreen from './ProductsScreen.jsx'
import ServicesScreen from './ServicesScreen.jsx'
import ProceduresScreen from './ProceduresScreen.jsx'
import PaymentsScreen from './PaymentsScreen.jsx'
import CreditsScreen from './CreditsScreen.jsx'
import RefundsScreen from './RefundsScreen.jsx'
import FinanceScreen from './FinanceScreen.jsx'
import ReconciliationScreen from './ReconciliationScreen.jsx'
import ExchangeRateControl from './ExchangeRateControl.jsx'

function LoginScreen({ onSession }) { const [email,setEmail]=useState('drrodolfocabezas@gmail.com'),[password,setPassword]=useState(''),[loading,setLoading]=useState(false),[error,setError]=useState(''); const submit=async(e)=>{e.preventDefault();setLoading(true);setError('');const{data,error:x}=await supabase.auth.signInWithPassword({email:email.trim(),password});if(x){setError('No pudimos iniciar sesión. Revisá el correo y la contraseña.');setLoading(false);return}onSession(data.session);setLoading(false)};return <Box minHeight="100vh" display="grid" sx={{placeItems:'center',p:2}}><Box component="form" onSubmit={submit} sx={{width:'100%',maxWidth:430,bgcolor:'background.paper',p:{xs:3,sm:4},borderRadius:3,boxShadow:'0 20px 60px rgba(20,20,60,.10)'}}><Stack spacing={3}><Box><Typography variant="overline" color="text.secondary">Gestión clínica</Typography><Typography variant="h4" fontWeight={800}>Dr. Rodolfo Cabezas</Typography></Box>{error&&<Alert severity="error">{error}</Alert>}<TextField label="Correo" type="email" value={email} onChange={e=>setEmail(e.target.value)} required/><TextField label="Contraseña" type="password" value={password} onChange={e=>setPassword(e.target.value)} required/><Button type="submit" variant="contained" disabled={loading}>{loading?'Ingresando…':'Iniciar sesión'}</Button></Stack></Box></Box> }
export default function ClinicApp(){const[session,setSession]=useState(undefined),[organization,setOrganization]=useState(null),[role,setRole]=useState(''),[section,setSection]=useState('clients'),[loading,setLoading]=useState(false),[error,setError]=useState('');useEffect(()=>{supabase.auth.getSession().then(({data})=>setSession(data.session));const{data:l}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>l.subscription.unsubscribe()},[]);useEffect(()=>{if(!session?.user?.id){setOrganization(null);setRole('');return}const go=async()=>{setLoading(true);const{data,error:e}=await supabase.from('organization_members').select('role,organization_id,organizations(id,name,slug,default_fx_crc_per_usd,settings)').eq('user_id',session.user.id).eq('active',true).limit(1);if(e||!data?.length)setError('Tu usuario no está asociado a una organización activa.');else{setRole(data[0].role);setOrganization(data[0].organizations)}setLoading(false)};go()},[session?.user?.id]);if(session===undefined||loading)return <Box minHeight="100vh" display="grid" sx={{placeItems:'center'}}><CircularProgress/></Box>;if(!session)return <LoginScreen onSession={setSession}/>;if(!organization)return <Container sx={{py:5}}>{error&&<Alert severity="error">{error}</Alert>}</Container>;return <Box minHeight="100vh"><AppBar position="sticky" color="inherit" elevation={0} sx={{borderBottom:'1px solid',borderColor:'divider'}}><Toolbar><Box flex={1}><Typography fontWeight={800}>{organization.name}</Typography><Typography variant="caption">{role==='admin'?'Administrador':'Asistente'}</Typography></Box><ExchangeRateControl organization={organization} userId={session.user.id} role={role} onUpdated={setOrganization}/><Button color="inherit" onClick={async()=>{await supabase.auth.signOut();setSession(null)}}>Salir</Button></Toolbar><Tabs value={section} onChange={(_,v)=>setSection(v)} variant="scrollable" scrollButtons="auto"><Tab value="clients" label="Clientes"/><Tab value="products" label="Productos"/><Tab value="services" label="Servicios"/><Tab value="procedures" label="Procedimientos"/><Tab value="payments" label="Pagos"/><Tab value="credits" label="Créditos"/>{role==='admin'&&<Tab value="refunds" label="Resolver pagos"/>}{role==='admin'&&<Tab value="finance" label="Finanzas"/>}{role==='admin'&&<Tab value="reconciliation" label="Conciliación"/>}</Tabs></AppBar><Container maxWidth="lg" sx={{py:{xs:3,sm:5}}}>{section==='clients'&&<ClientsModule organization={organization} userId={session.user.id} role={role}/>} {section==='products'&&<ProductsScreen organization={organization} userId={session.user.id} role={role}/>} {section==='services'&&<ServicesScreen organization={organization} userId={session.user.id} role={role}/>} {section==='procedures'&&<ProceduresScreen organization={organization} userId={session.user.id} role={role}/>} {section==='payments'&&<PaymentsScreen organization={organization} userId={session.user.id} role={role}/>} {section==='credits'&&<CreditsScreen organization={organization} userId={session.user.id} role={role}/>} {section==='refunds'&&role==='admin'&&<RefundsScreen organization={organization} userId={session.user.id}/>} {section==='finance'&&role==='admin'&&<FinanceScreen organization={organization} userId={session.user.id} role={role}/>} {section==='reconciliation'&&role==='admin'&&<ReconciliationScreen organization={organization} userId={session.user.id} role={role}/>}</Container></Box>}
