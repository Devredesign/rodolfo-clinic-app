import React,{useEffect,useMemo,useState}from'react';
import{Alert,Box,Button,Card,CardContent,Chip,Dialog,DialogActions,DialogContent,DialogTitle,Divider,IconButton,Stack,TextField,Tooltip,Typography}from'@mui/material';
import PaidIcon from'@mui/icons-material/Paid';import LockIcon from'@mui/icons-material/Lock';import EditIcon from'@mui/icons-material/Edit';import DeleteOutlineIcon from'@mui/icons-material/DeleteOutline';import RefreshIcon from'@mui/icons-material/Refresh';import{supabase}from'./supabase.js';
const money=(n,c='USD')=>new Intl.NumberFormat(c==='CRC'?'es-CR':'en-US',{style:'currency',currency:c}).format(Number(n||0));const fmt=d=>d?new Date(`${d}T12:00:00`).toLocaleDateString('es-CR'):'';const iso=d=>d.toISOString().slice(0,10);function currentWeek(){const d=new Date(),day=(d.getDay()+6)%7,s=new Date(d);s.setDate(d.getDate()-day);const e=new Date(s);e.setDate(s.getDate()+6);return{start:iso(s),end:iso(e)}}
const batchTone=s=>s==='closed'?{bg:'success.50',border:'success.light',chip:'success'}:s==='voided'?{bg:'error.50',border:'error.light',chip:'error'}:{bg:'warning.50',border:'warning.light',chip:'warning'};const transferColor=s=>s==='paid'?'success':s==='pending'?'warning':'default';
function TransferDialog({transfer,open,onClose,onSaved,organization}){const label=transfer?.direction==='clinic_to_rodolfo'?'Clínica → Rodolfo':'Rodolfo → Clínica';const[reference,setReference]=useState(''),[saving,setSaving]=useState(false),[error,setError]=useState('');useEffect(()=>{if(open){setReference(transfer?.reference||'');setError('')}},[open,transfer]);if(!transfer)return null;const save=async()=>{setSaving(true);const{error:e}=await supabase.rpc('set_reconciliation_transfer_status',{p_organization_id:organization.id,p_reconciliation_id:transfer.reconciliation_id,p_direction:transfer.direction,p_currency:transfer.currency,p_paid:true,p_reference:reference});if(e){setError('No se pudo registrar la transferencia.');setSaving(false);return}await onSaved();setSaving(false);onClose()};return <Dialog open={open} onClose={saving?undefined:onClose} fullWidth maxWidth="xs"><DialogTitle>Registrar transferencia</DialogTitle><DialogContent><Stack spacing={2} mt={1}>{error&&<Alert severity="error">{error}</Alert>}<Alert severity="info">{label} · {transfer.currency} · {money(transfer.amount,transfer.currency)}</Alert><TextField label="Referencia / comprobante" value={reference} onChange={e=>setReference(e.target.value)} fullWidth/></Stack></DialogContent><DialogActions><Button onClick={onClose}>Cancelar</Button><Button variant="contained" onClick={save} disabled={saving}>Marcar como pagado</Button></DialogActions></Dialog>}
function VoidDialog({batch,open,onClose,onSaved,organization,userId}){const[reason,setReason]=useState(''),[saving,setSaving]=useState(false),[error,setError]=useState('');useEffect(()=>{if(open){setReason('');setError('')}},[open]);if(!batch)return null;const save=async()=>{if(!reason.trim())return setError('Indicá el motivo de la anulación.');setSaving(true);const{error:e}=await supabase.rpc('void_weekly_reconciliation',{p_organization_id:organization.id,p_reconciliation_id:batch.id,p_reason:reason.trim(),p_voided_by:userId});if(e){setError('No se pudo anular la conciliación.');setSaving(false);return}await onSaved();setSaving(false);onClose()};return <Dialog open={open} onClose={saving?undefined:onClose} fullWidth maxWidth="sm"><DialogTitle>Anular conciliación</DialogTitle><DialogContent><Stack spacing={2} mt={1}>{error&&<Alert severity="error">{error}</Alert>}<Alert severity="warning">Quedará en el histórico y sus pagos volverán a estar disponibles.</Alert><TextField label="Motivo" value={reason} onChange={e=>setReason(e.target.value)} multiline minRows={3}/></Stack></DialogContent><DialogActions><Button onClick={onClose}>Cancelar</Button><Button color="error" variant="contained" onClick={save}>Anular</Button></DialogActions></Dialog>}
function EditRangeDialog({batch,open,onClose,onSaved,organization}){const[start,setStart]=useState(''),[end,setEnd]=useState(''),[saving,setSaving]=useState(false),[error,setError]=useState('');useEffect(()=>{if(open&&batch){setStart(batch.week_start);setEnd(batch.week_end);setError('')}},[open,batch]);if(!batch)return null;const save=async()=>{setSaving(true);const{error:e}=await supabase.rpc('recalculate_weekly_reconciliation',{p_organization_id:organization.id,p_reconciliation_id:batch.id,p_week_start:start,p_week_end:end});if(e){setError('No se pudo recalcular la conciliación.');setSaving(false);return}await onSaved();setSaving(false);onClose()};return <Dialog open={open} onClose={saving?undefined:onClose} fullWidth maxWidth="xs"><DialogTitle>Editar período</DialogTitle><DialogContent><Stack spacing={2} mt={1}>{error&&<Alert severity="error">{error}</Alert>}<TextField label="Inicio" type="date" value={start} onChange={e=>setStart(e.target.value)} InputLabelProps={{shrink:true}}/><TextField label="Fin" type="date" value={end} onChange={e=>setEnd(e.target.value)} InputLabelProps={{shrink:true}}/></Stack></DialogContent><DialogActions><Button onClick={onClose}>Cancelar</Button><Button variant="contained" onClick={save}>Guardar y recalcular</Button></DialogActions></Dialog>}
export default function ReconciliationScreen({organization,userId,role}){const w=currentWeek();const[start,setStart]=useState(w.start),[end,setEnd]=useState(w.end),[batches,setBatches]=useState([]),[transfers,setTransfers]=useState([]),[selected,setSelected]=useState(null),[transfer,setTransfer]=useState(null),[voiding,setVoiding]=useState(null),[editing,setEditing]=useState(null),[error,setError]=useState(''),[creating,setCreating]=useState(false);const load=async()=>{const[b,t]=await Promise.all([supabase.from('reconciliation_batches').select('*').eq('organization_id',organization.id).order('week_start',{ascending:false}),supabase.from('reconciliation_transfers').select('*').eq('organization_id',organization.id).order('currency')]);if(b.error||t.error){setError('No se pudieron cargar las conciliaciones.');return}setBatches(b.data||[]);setTransfers(t.data||[]);setSelected(cur=>cur?(b.data||[]).find(x=>x.id===cur.id)||null:(b.data||[])[0]||null)};useEffect(()=>{load()},[organization.id]);const selectedTransfers=useMemo(()=>selected?transfers.filter(t=>t.reconciliation_id===selected.id):[],[transfers,selected]);const canClose=selected&&selected.status==='open'&&selectedTransfers.length>0&&selectedTransfers.every(t=>['paid','not_required'].includes(t.status));const create=async()=>{setCreating(true);setError('');const{data,error:e}=await supabase.rpc('create_weekly_reconciliation',{p_organization_id:organization.id,p_week_start:start,p_week_end:end,p_created_by:userId});if(e){const msg=String(e.message||'').toLowerCase();setError(msg.includes('already exists')||msg.includes('period')?'Estas fechas ya están conciliadas. Anulá la conciliación existente o elegí otro período.':'No se pudo generar la conciliación.');setCreating(false);return}await load();setSelected(data);setCreating(false)};const recalc=async()=>{const{data,error:e}=await supabase.rpc('recalculate_weekly_reconciliation',{p_organization_id:organization.id,p_reconciliation_id:selected.id,p_week_start:null,p_week_end:null});if(e)return setError('No se pudo recalcular.');await load();setSelected(data)};const close=async()=>{const{data,error:e}=await supabase.rpc('close_weekly_reconciliation',{p_organization_id:organization.id,p_reconciliation_id:selected.id,p_closed_by:userId});if(e)return setError('No se pudo cerrar. Revisá las transferencias.');await load();setSelected(data)};const cards=selected?[['Cobros USD',money(selected.eligible_revenue_usd,'USD')],['Cobros CRC',money(selected.eligible_revenue_crc,'CRC')],['IVA USD',money(selected.vat_cost_usd,'USD')],['IVA CRC',money(selected.vat_cost_crc,'CRC')],['Comisiones USD',money(selected.processor_fees_usd,'USD')],['Comisiones CRC',money(selected.processor_fees_crc,'CRC')],['Productos',money(selected.product_cost_usd,'USD')],['Margen equiv. USD',money(selected.rodolfo_margin_usd,'USD')]]:[];const batchLabel=b=>b.status==='closed'?'Cerrada':b.status==='voided'?'Anulada':'Abierta';const selectedTone=selected?batchTone(selected.status):null;return <Stack spacing={3}><Box><Typography variant="h4" fontWeight={800}>Conciliación</Typography><Typography color="text.secondary">Cobros y transferencias separados por USD y CRC.</Typography></Box>{error&&<Alert severity="error" onClose={()=>setError('')}>{error}</Alert>}<Card variant="outlined"><CardContent><Stack direction={{xs:'column',md:'row'}} spacing={2}><TextField label="Inicio" type="date" value={start} onChange={e=>setStart(e.target.value)} InputLabelProps={{shrink:true}}/><TextField label="Fin" type="date" value={end} onChange={e=>setEnd(e.target.value)} InputLabelProps={{shrink:true}}/><Button variant="contained" onClick={create} disabled={creating}>{creating?'Generando…':'Generar conciliación'}</Button></Stack></CardContent></Card>{selected&&<><Card variant="outlined"><CardContent sx={{p:{xs:1.5,sm:2}}}><Box sx={{display:'grid',gridTemplateColumns:{xs:'1fr 1fr',sm:'repeat(4,1fr)'},columnGap:2,rowGap:{xs:1,sm:2}}}>{cards.map(([l,v])=><Box key={l} sx={{minWidth:0}}><Typography variant="caption" color="text.secondary" display="block" noWrap>{l}</Typography><Typography variant={{xs:'body2',sm:'h6'}} fontWeight={800} noWrap>{v}</Typography></Box>)}</Box></CardContent></Card><Card variant="outlined" sx={{bgcolor:selectedTone.bg,borderColor:selectedTone.border}}><CardContent><Stack spacing={2}><Stack direction="row" justifyContent="space-between" spacing={1}><Box><Typography fontWeight={800}>Semana {fmt(selected.week_start)} – {fmt(selected.week_end)}</Typography><Typography variant="body2" color="text.secondary">{batchLabel(selected)}</Typography></Box><Stack direction="row" spacing={.25}><Chip size="small" color={selectedTone.chip} label={batchLabel(selected)}/>{selected.status==='open'&&<><Tooltip title="Editar"><IconButton size="small" onClick={()=>setEditing(selected)}><EditIcon/></IconButton></Tooltip><Tooltip title="Recalcular"><IconButton size="small" onClick={recalc}><RefreshIcon/></IconButton></Tooltip></>} {selected.status!=='voided'&&<Tooltip title="Anular"><IconButton size="small" color="error" onClick={()=>setVoiding(selected)}><DeleteOutlineIcon/></IconButton></Tooltip>}</Stack></Stack><Divider/><Typography fontWeight={800}>Transferencias por moneda</Typography><Stack spacing={1.5}>{selectedTransfers.map(t=><TransferRow key={t.id} transfer={t} onPay={()=>setTransfer(t)} locked={selected.status!=='open'}/>)}</Stack>{selected.status==='open'&&<><Divider/><Button startIcon={<LockIcon/>} variant="contained" color="success" disabled={!canClose} onClick={close}>Conciliar semana</Button></>}</Stack></CardContent></Card></>}<Card variant="outlined"><CardContent sx={{p:0}}>{batches.map((b,i)=>{const tone=batchTone(b.status);return <Box key={b.id}>{i>0&&<Divider/>}<Box p={2} onClick={()=>setSelected(b)} sx={{cursor:'pointer',bgcolor:tone.bg,borderLeft:4,borderColor:tone.border,'&:hover':{filter:'brightness(.98)'}}}><Stack direction="row" justifyContent="space-between"><Box><Typography fontWeight={700}>{fmt(b.week_start)} – {fmt(b.week_end)}</Typography><Typography variant="caption">USD {money(b.eligible_revenue_usd,'USD')} · CRC {money(b.eligible_revenue_crc,'CRC')}</Typography></Box><Chip size="small" color={tone.chip} label={batchLabel(b)}/></Stack></Box></Box>})}</CardContent></Card><TransferDialog transfer={transfer} open={!!transfer} onClose={()=>setTransfer(null)} onSaved={load} organization={organization}/><VoidDialog batch={voiding} open={!!voiding} onClose={()=>setVoiding(null)} onSaved={load} organization={organization} userId={userId}/><EditRangeDialog batch={editing} open={!!editing} onClose={()=>setEditing(null)} onSaved={load} organization={organization}/></Stack>}
function TransferRow({transfer,onPay,locked}){const label=transfer.direction==='clinic_to_rodolfo'?'Clínica → Rodolfo':'Rodolfo → Clínica';return <Stack direction="row" justifyContent="space-between" alignItems="center" spacing={1}><Box><Typography fontWeight={700}>{label} · {transfer.currency}</Typography><Typography variant="body2" color="text.secondary">{money(transfer.amount,transfer.currency)}</Typography></Box><Stack direction="row" alignItems="center"><Chip size="small" color={transferColor(transfer.status)} label={transfer.status==='paid'?'Pagado':transfer.status==='not_required'?'No corresponde':'Pendiente'}/>{transfer.status==='pending'&&!locked&&<IconButton color="success" size="small" onClick={onPay}><PaidIcon/></IconButton>}</Stack></Stack>}
